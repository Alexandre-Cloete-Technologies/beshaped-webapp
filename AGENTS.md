# AGENTS.md (for Cursor and other agents)

All project instructions live in CLAUDE.md files (shared with Claude Code). Before any task read:
1. ../CLAUDE.md (workspace: repos, shared Firebase backend, cross-repo rules, Notion links)
2. ./CLAUDE.md (this repo: stack, commands, conventions)
Follow them exactly as if they were written here.
